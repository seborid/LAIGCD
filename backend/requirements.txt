# LAIGCD 后端API依赖
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 项目核心依赖
open-clip-torch>=2.23.0
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Pillow>=10.0.0
matplotlib>=3.7.0

# 其他工具
scikit-learn>=1.3.0

# 第二阶段 FakeVLM（LLaVA-1.5-7B）推理依赖
transformers>=4.45.0      # LlavaForConditionalGeneration / AutoProcessor
bitsandbytes>=0.43.0      # 8-bit 量化（12GB 显存下加载 7B 模型必需）
accelerate>=0.30.0        # device_map="auto" + 8-bit 加载所需
sentencepiece>=0.2.0      # LLaVA 分词器
einops>=0.8.0

