# LAIGCD Requirements

# PyTorch (根据CUDA版本选择)
torch>=2.0.0
torchvision>=0.15.0

# CLIP模型
open-clip-torch>=2.23.0

# 图像处理
Pillow>=9.0.0
opencv-python>=4.7.0

# 频域处理（可选）
PyWavelets>=1.4.0

# 数据处理
numpy>=1.24.0
scikit-learn>=1.2.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# 进度条
tqdm>=4.65.0

# Transformers（用于替代的CLIP加载方式）
transformers>=4.45.0

# 模型优化
timm>=0.9.0

# ================================
# FakeVLM 集成依赖
# ================================

# Transformers (更新版本用于LLaVA)
transformers>=4.45.0

# Flash Attention 2 (可选，用于加速)
# flash-attn>=2.0.0  # 需要CUDA环境，安装: pip install flash-attn --no-build-isolation

# 量化支持 (可选)
# bitsandbytes>=0.41.0

# 加速库 (可选)
# accelerate>=0.20.0

# 多模态处理器
# sentencepiece>=0.1.99
